# Broker: (research baseline) / platform: (none)
#
# Generated by scripts/migrate_broker_fees.py from config/broker_fees.toml.
# The flat broker_fees.toml is still authoritative for the legacy
# src.backtest.fee_profiles registry; this file is the broker-first view
# used by src.fees.FeeManager.
#
# Scenarios derived during migration: illiquid (normal × 3.5) and
# volatile (normal × 2.0) — refined over time from the weekly spread
# sampler + fee_calibration/*.json empirical overrides.

[broker]
id = "pine_zero_cost"
name = "(research baseline)"
platform = "(none)"
regulator = "(n/a)"
country = "(n/a)"
api_type = "(none)"
account_types = []
min_deposit_usd = 0.0
base_currencies = []
notes = """
Zero-cost research baseline — matches TradingView strategy tester
defaults for Pine Script port validation (Stage 0 of the strategy
dev process). NOT for production backtests. After port equivalence,
switch to a real broker profile for Phase 2 viability testing.
"""

[instruments.any]
symbol_aliases = []
contract_size = 100.0
min_lot = 0.01
max_lot = 100.0
lot_step = 0.01

[instruments.any.scenarios.pine_faithful]
description = "Zero commission, zero spread, zero slippage — matches TradingView strategy tester defaults for Pine port validation"
notes = """
Use this profile to reproduce TradingView's strategy tester output
during Stage 0 Pine Script port validation. NOT for production
backtests. After validating port equivalence, switch to a real broker
profile for Phase 2 (strategy viability).
"""

[instruments.any.scenarios.pine_faithful.spread]
base_spread_pips = 0.0
normal_slip_pips = 0.0
atr_vol_mult = 0.0
news_spread_mult = 1.0
news_slip_mult = 1.0
pip_size = 0.1

[instruments.any.scenarios.pine_faithful.commission]
type = "fixed_per_lot"
per_lot_per_side_usd = 0.0
contract_size = 100.0

